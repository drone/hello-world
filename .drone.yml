kind: pipeline
name: default

clone:
  disable: true

steps:
- name: clone
  image: docker:git
  commands:
  - git clone https://github.com/octocat/hello-world.git
  - cd hello-world
  - git checkout $DRONE_COMMIT

- name: build
  image: alpine:3.8
  commands:
  - cd hello-world
  - ls -la

